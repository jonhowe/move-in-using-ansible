# Copy this file to .env.yml and override only the values you want to change.
# The file is YAML even though the name starts with ".env" so nested Ansible
# variables remain easy to override.

user: jhowe
group: jhowe
home_directory: /home/jhowe

docker_add_user_to_group: true
downloads_migrate_existing: true

git_user_name: Jon Howe
git_user_email: howe.jon@gmail.com

pipx_packages:
  - httpie

applications:
  - name: signal-desktop
    repo: "deb [arch=amd64] https://updates.signal.org/desktop/apt xenial main"
    key_url: https://updates.signal.org/desktop/apt/keys.asc

snaps_regular:
  - brave
  - drawing
  - remmina
  - plex-desktop
  - bitwarden

joplin:
  enabled: true
  install_method: snap
  snap_name: joplin-desktop
  classic: false
  channel: stable

zoom:
  enabled: true
  install_method: snap
  snap_name: zoom-client
  classic: false
  channel: stable

rambox:
  enabled: true
  install_method: snap
  snap_name: rambox
  classic: false
  channel: stable

synaptics_repository_keyring_deb_checksum: ""
